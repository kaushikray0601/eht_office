# idfviewer Progress Tracker - 2026-06-18

## Done

- Created recovery snapshot before this workstream:
  - Branch: `snapshot/mvp-before-idfviewer-3d-2026-06-18`
  - Tag: `snapshot-mvp-before-idfviewer-3d-2026-06-18`
- Reviewed backend parsers, persistence, views, templates, frontend viewer, tests, and related notes.
- Added in-app record folders for audits, planning, tracking, and decisions.
- Restored idfviewer view-test compatibility with global login middleware by authenticating `SavedPipelineFlowTests`.
- Verified `venv/bin/python manage.py test idfviewer -v 2 --noinput` passes: 17 tests run, 16 passed, 1 skipped because the optional sample IFC file is absent.
- Added the first EHT-oriented viewer tools:
  - PCF `ATTRIBUTE##` display-name settings in the viewer menubar.
  - Right-pane "Project Attributes" display using user-defined labels with raw-key fallback.
  - Ground-grid numeric scale labels for X/Y plan coordinates.
  - Fold/expand controls for hierarchy file/group rows without changing checkbox visibility semantics.
- Promoted PCF attribute names from browser-only storage to a project-scoped database table:
  - Added `ProjectAttributeMapping`.
  - Added `/idfviewer/projects/<project_id>/attribute-mappings/` JSON API.
  - Applied migration `idfviewer.0005_projectattributemapping`.
  - Changed the right-pane project-attribute UI to show one attribute by default and add more via a small `+` control.
  - Added a scale-label toolbar toggle and right properties panel hide/show controls.
- Started measurement-ready coordinate handling:
  - Added shared unit metadata helpers for scene normalization.
  - PCF now reads `UNITS-CO-ORDS` when present and records unit confidence.
  - IDF currently records an assumed millimetre coordinate basis.
  - IFC currently records an assumed metre coordinate basis.
  - Grid sizing now follows the visible geometry extent instead of a fixed 1 m x 1 m appearance.
  - Scale labels now follow the scene display unit and adaptive grid step.
  - Verified `venv/bin/python manage.py test idfviewer -v 2 --noinput` passes: 19 tests run, 18 passed, 1 skipped because the optional sample IFC file is absent.
- Added the first interactive measurement pass:
  - Added a topbar Measure mode.
  - Added a visible coordinate-basis badge showing source format, coordinate unit, display unit, unit confidence, and grid step.
  - Added endpoint/point snapping for pipes, fittings, welds, supports, and markers.
  - Added two-point measurement markers, line, in-scene length label, and measurement HUD.
  - Added first-pass PCF `CUT-PIECE-LENGTH` mismatch detection when measuring start-to-end on the same pipe or fitting item.
  - Bumped the viewer static asset version so browsers fetch the new grid/unit/measurement logic.
- Refined the first measurement/grid UI pass:
  - Restored a generous review grid footprint for IDF/PCF scenes so small isometrics do not sit on a cramped grid.
  - Removed floating rectangular grid scale labels; the coordinate badge is now the scale readout.
  - Changed the scale toggle into a grid/axes toggle.
  - Compact-formatted the coordinate scale readout, e.g. `0.001 m per MM`.
  - Reduced measurement snap marker size and changed it from black to a calmer cyan tone.
- Polished measurement presentation:
  - Fixed the active Measure button state so the label remains visible.
  - Changed snap markers and measurement line to orange for better contrast against dark pipe geometry.
  - Replaced the heavy in-scene measured-length badge with a smaller, softer engineering tag.
  - Bumped the viewer static asset version to fetch the polished measurement UI.
- Refined snap selection:
  - Added hover snap preview in Measure mode.
  - Shows a small orange ring and soft `Snap:` label before click selection.
  - Clears preview on click, pointer leave, mode exit, and Escape.
  - Bumped the viewer static asset version for the hover-snap pass.
- Started the manual EHT design authoring layer:
  - Added persistent `EHTDesignElement` records scoped by project and optionally saved IDF/PCF file.
  - Added `/idfviewer/projects/<project_id>/eht-elements/` JSON load/save API.
  - Applied migration `idfviewer.0006_ehtdesignelement`.
  - Added first-pass EHT tool palette for distribution boards, junction boxes, isolators, SR/MI tracers, RTDs, cold cable, end terminations, and pipe straps.
  - Point tools place one element; tracer/cold-cable tools place a two-point polyline.
  - Elements render as colored EHT overlay geometry and can be selected from the viewer.
  - Right properties panel can edit label and metadata fields, delete elements, and save the EHT layer.
  - Added backend tests for EHT layer save/load and geometry validation.
- Hardened the EHT authoring architecture:
  - Moved the authoritative EHT tool catalogue and metadata schema into backend code.
  - EHT API now returns tool definitions with element labels, geometry type, colors, and field schemas.
  - Frontend consumes backend definitions for defaults and edit forms, keeping the viewer as an interaction layer rather than the source of truth.
  - Backend validates metadata values, dropdown choices, field lengths, geometry type, and geometry points before persistence.
  - Added tests for returned tool definitions and metadata schema validation.
- Advanced routing-style EHT placement:
  - SR tracer, MI tracer, and cold-cable tools now support multi-point route placement instead of only two-click segments.
  - Added Finish Route and Cancel controls plus Enter/Space and double-click finishing.
  - Added route preview markers and line while placing a route.
  - Frontend shows immediate route length/segment count in the EHT properties panel.
  - Backend recomputes and stores route `length_m`, `segment_count`, and `point_count` on save/load so quantities are not trusted only to frontend calculations.
  - Added tests for backend route metrics.
- Added EHT geometry editing:
  - Point elements can be moved to a new snapped or grid-plane position.
  - Polyline routes can move a selected vertex, add a vertex after the selected point, or delete a vertex while preserving at least two route points.
  - Route length and segment count update immediately in the properties panel and remain backend-normalized on save/load.
  - Geometry edit commands use click-to-apply behavior instead of continuous dragging to keep interaction stable on large scenes.
- Added EHT overlay hierarchy/layer controls:
  - Left hierarchy now includes a dedicated EHT Overlay section once design elements exist.
  - EHT elements are grouped by type with type-level visibility toggles.
  - Individual EHT elements can be hidden/shown and selected from the hierarchy.
  - EHT rows participate in hierarchy search and search result counts.
  - EHT hierarchy rebuilds independently from the main plant hierarchy to avoid unnecessary DOM churn on large scenes.
  - Viewer asset version bumped to fetch the hierarchy/layer-control pass.
- Polished the EHT/3D workspace controls:
  - Moved the collapsed-left `Show Assets` control below the coordinate-basis card so the two no longer overlap.
  - Added a fold/show control to the EHT tool palette so the authoring controls can be rolled up for more 3D viewport space.
  - Added EHT overlay selection highlighting for point symbols, route vertices, and route lines.
  - Added a `Focus` action in the EHT properties panel to move the camera to the selected EHT element.
  - Kept normal plant-item selection and EHT overlay selection mutually clear so the right property panel reflects one active object.
- Refined EHT authoring usability:
  - Aligned the collapsed `Show Assets` button with the coordinate-basis card and bumped the viewer asset version for the palette fold fix.
  - Changed the EHT Overlay `Show All` button into a `Show All` / `Hide All` toggle.
  - Added confirmed persistent deletion for EHT elements through the backend layer save path.
  - Wired the keyboard Delete/Backspace shortcut to delete the selected EHT element when the user is not typing.
  - Added snap-aware placement preview for EHT tools and geometry edits.
  - Added temporary route-leg preview and live leg length labels while placing tracer and cold-cable routes.
- Tightened EHT overlay tree and route-start interactions:
  - Moved EHT Delete/Backspace handling to document-level capture so keyboard deletion works even when the canvas itself does not have focus.
  - Fixed `Hide All` so EHT type headers and child rows are both unchecked.
  - Fixed child-row re-selection after `Hide All`; checking an individual EHT element now re-enables its type.
  - Added collapsible EHT type groups using compact `-` / `+` controls.
  - Added `Start Route From Here` actions on selected point elements for cold cable, SR tracer, and MI tracer.
  - Routes started from an EHT element now begin at that element anchor and write first-pass connection metadata on completion.
- Refined EHT component placement and presentation:
  - Reduced EHT label size and changed EHT labels to a sharper, quieter translucent label style.
  - Added DB/JB dimension metadata fields: length, width, and height in metres.
  - Rendered DB/JB as metadata-sized cuboids, with distribution boards defaulting larger than junction boxes.
  - Treated DB/JB geometry points as the cuboid front-left-lower coordinate.
  - Added coordinate entry fields for point EHT elements so users can place components at known X/Y/Z positions.
  - Added small X/Y/Z nudge controls for fine movement after snap placement.
  - Added soft connection-target preview while drawing from an EHT element, including target labels near nearby candidate elements.

## Active Todo

- Reconcile old root-level notes with this in-app baseline.
- Add native IFC unit extraction from IFC unit assignments instead of assuming metres.
- Refine EHT authoring UX with persistent connection validation, typed dropdowns, and better metadata dictionaries.
- Add export/report summary for EHT overlay quantities by type/circuit.
- Refine the measuring tool with clearer tolerance settings and broader file-parameter comparisons.
- Preserve uploaded folder paths/provenance instead of basename-only filenames.
- Align project selection/scoping with the main EHT managed-project model.
- Decide whether plot-plan overlay persistence should be completed or explicitly documented as browser-only.

## Deferred

- IFC persistence model and storage strategy.
- Local/offline packaging for Three.js/Tailwind assets.
- Scene-wide UID strategy for large combined batches.
- Performance work such as `InstancedMesh` and geometry batching.
